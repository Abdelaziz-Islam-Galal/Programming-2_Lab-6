@startuml
title Activity Diagram: Enrollment in a Course

|Student|
start

:Enter Login Credentials;

|System|
:Authenticate User;

if (Credentials Valid?) then (yes)
  :Grant Access to Platform;
else (no)
  |Student|
  :Display Message:Wrong Credentials;
  stop
endif

|Student|
:View Course List;

|System|
:Fetch Available Courses from Database;
:Display Course Catalog;

|Student|
:Select a Course;

|System|
:Load Course Details;
:Display Course Information;
note right
  Instructor Name
  Number of Lessons
  Course Duration
end note

|Student|
if (Confirm Enrollment?) then (no)
  stop
else (yes)
  :Click Enroll Button;
endif

|System|
fork
  :Check Course Capacity;
fork again
  :Check Enrollment Status;
end fork

if (Course Full?) then (yes)
  |Student|
  :Display Message: Course is Full;
  stop
endif
|System|
if (Already Enrolled?) then (yes)
  |Student|
  :Display Message: Already Enrolled;
  stop
endif

|System|
:Create Enrollment Record;
:Add Student to Course;
:Increment Enrollment Count;
:Save to JSON Database;

if (Save Successful?) then (no)
  |Student|
  :Display Enrollment Failed Message;
  stop
else (yes)
  :Display Success Message;
endif

|Student|

:Navigate to Course Dashboard;

stop
@enduml